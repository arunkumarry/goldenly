development:
  adapter: async

test:
  adapter: test

production:
  # Goldenly currently uses request/response Turbo Streams rather than
  # broadcast channels. Avoid introducing Redis until real-time broadcasts
  # are needed in production.
  adapter: async
